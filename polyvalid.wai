// validate name for package, namespace, user, app

record output {
       is-valid: bool,
       error-message: string // empty if is_valid is true. When is_valid is false, contains error message
}

is-app-name-valid: func (name: string) -> output
